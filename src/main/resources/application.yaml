server:
  port: 8080

spring:
  application:
    name: rah-admin-server

  security:
    user:
      name: ${USER_NAME} 
      password: ${USER_PASSWORD}

  boot:
    admin:
      #context-path: /monitoreo-bluprint # cambiar la url inicial
      ui:
        title: "RAH Admin Server"
